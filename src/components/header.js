import React from 'react';

const Header = (title) => {
    return (
        <div className="header">
            <h1>{title}</h1>
            <p>ayy yo</p>
        </div>
    )
}

export default Header;
